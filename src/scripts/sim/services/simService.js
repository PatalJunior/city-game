import { City } from '../city.js';

/**
 * Um serviço representa a lógica de simulação aplicada ao nível da cidade.
 */
export class SimService {
  /**
   * @param {City} city 
   */
  simulate(city) {
    // Implementar em subclasse
  }
}